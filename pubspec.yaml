name: alertx
description: "AlertX WebView Application"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

# 👇 تنظیمات آیکن (خیلی مهم)
flutter_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon.png"
